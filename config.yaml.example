server:
  host: "0.0.0.0"
  port: 8080

reindexer:
  dsn: "cproto://localhost:6534/db"
  namespace: "default"
  max_connections: 10

cache:
  shards: 16
  ttl: 900  # 15 minutes in seconds

concurrency:
  http_max_workers: 100
  processor_workers: 10
  cache_shards: 16
  db_max_connections: 10

